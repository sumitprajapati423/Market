import { ShareDirective } from './share.directive';

describe('ShareDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareDirective();
    expect(directive).toBeTruthy();
  });
});
