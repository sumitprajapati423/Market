// import { Directive, Input, Output, EventEmitter } from '@angular/core';

// @Directive({
//   selector: '[shareButton]'
// })
// export class ShareDirective {
//   @Input('shareButtonName') shareButtonName: string;
//   @Input('autoSetMeta') autoSetMeta: boolean;
//   @Input('url') url: string;
//   @Input('title') title: string;
//   @Input('image') image: string;
//   @Input('redirectUrl') redirectUrl: string;
//   @Output('opened') opened: EventEmitter<any> = new EventEmitter();
//   @Output('closed') closed: EventEmitter<any> = new EventEmitter();

//   constructor() { }

//   ngOnInit(): void {
//     // Initialize the directive here
//   }

//   ngOnChanges(changes: SimpleChanges): void {
//     // Handle changes to input properties here
//   }

//   ngOnDestroy(): void {
//     // Clean up resources here
//   }
// }

