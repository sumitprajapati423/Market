import { ShareDirectiveDirective } from './share-directive.directive';

describe('ShareDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
